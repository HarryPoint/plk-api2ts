// JSONResult«List«设备选择返回VO - 针对数采方案»»
export interface IJSONResultList设备选择返回VO针对数采方案 {
    // 返回码
    code: number;
    // 返回消息说明
    msg: string;
    // 响应结果
    data: 设备选择返回VO - 针对数采方案[];
    // 服务器结果返回时的 Unix timestamp,单位毫秒
    ts: number;
}
