// JSONResult«项目变更信息校验结果»
export interface IJSONResult项目变更信息校验结果 {
    // 返回码
    code: number;
    // 返回消息说明
    msg: string;
    // 响应结果
    data: 项目变更信息校验结果;
    // 服务器结果返回时的 Unix timestamp,单位毫秒
    ts: number;
}
